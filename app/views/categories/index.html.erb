<div class="category-container">

  <div class="side-bar">
    <div class="side-bar-create-btn">
      <%= link_to "単語帳を作成する", "#" %>
    </div>

    <div class="categories">
      <%# <% current_user.categories.each do |category| %> 
        <div class="category">
          <div class="category-name">
            <%# <%= link_to category.name, room_messages_path(room) %>
          </div>
        </div>
      <%# <% end %>
    </div>

  </div>

  <div class="main-register">
  </div>
</div>